install git (and Xcode commandline tools)
install config

install homebrew
  brew update
  brew doctor

brew install easy_install?

sudo easy_install pip
sudo pip install ipython[notebook]
sudo pip install fs
sudo pip install awscli
sudo pip install paramiko pyyaml jinja2 httplib2 passlib nose boto
sudo pip install beautifulsoup4
g clone git@github.com:ansible/ansible.git

install xquartz

brew install
  reattach-to-user-namespace
  tmux
  htop
  ctags-exuberant
  hg
  bazaar
  go
  node
  pypy
  haskell-platform
  swi-prolog
  erlang
  leiningen
  scala
  sbt
  python3
  coq
  maven
  s3cmd
  snappy
  gradle
  spidermonkey
  markdown
  colout
  watch
  mtr (traceroute/ping hybrid thing)
  ansible

sudo pip install python-snappy
sudo pip install chardet

git@github.com:davidledwards/zookeeper.git

npm install -g coffee-script

install racket manually
raco pkg install git://github.com/gregr/racket-misc

cabal update
cabal install agda hlist lens
add ~/.cabal/bin to $PATH

brew install go hg bazaar

set up $GOPATH

if ctags version < 5.9, write to ~/.ctags:
--langdef=Go
--langmap=Go:.go
--regex-Go=/func([ \t]+\([^)]+\))?[ \t]+([a-zA-Z0-9_]+)/\2/d,func/
--regex-Go=/var[ \t]+([a-zA-Z_][a-zA-Z0-9_]+)/\1/d,var/
--regex-Go=/type[ \t]+([a-zA-Z_][a-zA-Z0-9_]+)/\1/d,type/
